// Question - E

#include<bits/stdc++.h>
using namespace std;
#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define endl '\n'

int main()
{
    optimize();

    double a, pi = 3.141592653;
    cin >> a;
    double area = pi*a*a;
    cout << fixed << setprecision(9) << area << endl;
    return 0;
}
